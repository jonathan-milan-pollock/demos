describe('/destination', () => {
  beforeEach(() => {
    cy.visit('/destination');
  });

  // eslint-disable-next-line @typescript-eslint/no-empty-function
  it('Should allow navigating to the Destination page', () => {});
});

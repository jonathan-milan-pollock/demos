describe('/photo-of-the-week', () => {
  beforeEach(() => {
    cy.visit('/photo-of-the-week');
  });

  // eslint-disable-next-line @typescript-eslint/no-empty-function
  it('Should allow navigating to the Photo of the Week page', () => {});
});

describe('/photo-of-the-week/image', () => {
  beforeEach(() => {
    cy.visit('/photo-of-the-week/image');
  });

  // eslint-disable-next-line @typescript-eslint/no-empty-function
  it('Should allow navigating to the Photo of the Week Image page', () => {});
});

describe('/story', () => {
  beforeEach(() => {
    cy.visit('/story');
  });

  // eslint-disable-next-line @typescript-eslint/no-empty-function
  it('Should allow navigating to the Story page', () => {});
});

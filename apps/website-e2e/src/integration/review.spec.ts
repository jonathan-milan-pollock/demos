describe('/review', () => {
  beforeEach(() => {
    cy.visit('/review');
  });

  // eslint-disable-next-line @typescript-eslint/no-empty-function
  xit('Should allow navigating to the Destinations Admin page', () => {});
});

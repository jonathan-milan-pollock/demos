describe('/stories', () => {
  beforeEach(() => {
    cy.visit('/stories');
  });

  // eslint-disable-next-line @typescript-eslint/no-empty-function
  it('Should allow navigating to the Stories page', () => {});
});

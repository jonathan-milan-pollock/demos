describe('/reviews', () => {
  beforeEach(() => {
    cy.visit('/reviews');
  });

  // eslint-disable-next-line @typescript-eslint/no-empty-function
  it('Should allow navigating to the Reviews page', () => {});
});

import { serverlessFeatureSocialMediaPost } from './serverless-feature-social-media-post';

describe('serverlessFeatureSocialMediaPost', () => {
  it('should work', () => {
    expect(serverlessFeatureSocialMediaPost()).toEqual(
      'serverless-feature-social-media-post'
    );
  });
});

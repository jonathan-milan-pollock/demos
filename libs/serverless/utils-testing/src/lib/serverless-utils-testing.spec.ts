import { serverlessUtilsTesting } from './serverless-utils-testing';

describe('serverlessUtilsTesting', () => {
  it('should work', () => {
    expect(serverlessUtilsTesting()).toEqual('serverless-utils-testing');
  });
});

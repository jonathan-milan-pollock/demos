describe('media-process-type.functions', () => {
  describe('findEntityTypeFromMediaProcessType', () => {
    /* it('should return media apple icon document type for apple icon media type', () => {
      const mediaProcessType = MediaProcessType.Icon;
      const entityType = EntityType.MediaProcessIcon;

      const result = findEntityTypeFromMediaProcessType(mediaProcessType);
      expect(result).toBe(entityType);
    });

    it('should return media apple resource document type for apple resource media type', () => {
      const mediaProcessType = MediaProcessType.AppleResource;
      const entityType = EntityType.MediaProcessAppleResource;

      const result = findEntityTypeFromMediaProcessType(mediaProcessType);
      expect(result).toBe(entityType);
    });

    it('should return media image video document type for image video media type', () => {
      const mediaProcessType = MediaProcessType.CreateImageVideo;
      const entityType = EntityType.MediaProcessImageVideo;

      const result = findEntityTypeFromMediaProcessType(mediaProcessType);
      expect(result).toBe(entityType);
    });

    it('should return media mobile image document type for mobile image media type', () => {
      const mediaProcessType = MediaProcessType.PostMobileImage;
      const entityType = EntityType.MediaProcessPostMobileImage;

      const result = findEntityTypeFromMediaProcessType(mediaProcessType);
      expect(result).toBe(entityType);
    });

    it('should return media png document type for png media type', () => {
      const mediaProcessType = MediaProcessType.Png;
      const entityType = EntityType.MediaProcessPng;

      const result = findEntityTypeFromMediaProcessType(mediaProcessType);
      expect(result).toBe(entityType);
    });

    it('should throw a bad request exception if the media type is not valid', () => {
      const mediaProcessType = '' as MediaProcessType;
      expect(() => {
        findEntityTypeFromMediaProcessType(mediaProcessType);
      }).toThrow(BadRequestException);
    });

    it('should throw message unable to find media type when not valid', () => {
      const mediaProcessType = 'invalidMediaProcessType' as MediaProcessType;
      expect(() => {
        findEntityTypeFromMediaProcessType(mediaProcessType);
      }).toThrow(`Unable to find media type invalidMediaProcessType`);
    });*/
  });
});

import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import { FormGroup, FormControl } from '@angular/forms';

import { FormDocumentMetadataGroupComponent } from './form-document-metadata-group.component';
import { WebsiteUiUiAdminModule } from '../website-ui-ui-admin.module';

<Meta
  title="UI ADMIN/Form Document Metadata Group"
  component={FormDocumentMetadataGroupComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiUiAdminModule],
    }),
  ]}
/>

export const metadataGroup = new FormGroup({
  title: new FormControl(null),
  description: new FormControl(null),
  keywords: new FormControl(null),
  dateCreated: new FormControl(null),
  datePublished: new FormControl(null),
});
export const titleFormControlName = 'title';
export const descriptionFormControlName = 'description';
export const keywordsFormControlName = 'keywords';
export const dateCreatedFormControlName = 'dateCreated';
export const datePublishedFormControlName = 'datePublished';

# Form Metadata Group

<Story
  height="500px"
  name="Default"
  args={{
    titleFormControlName,
    descriptionFormControlName,
    keywordsFormControlName,
    dateCreatedFormControlName,
    datePublishedFormControlName,
  }}
>
  {(args) => (
    <drp-form-document-metadata-group {...args} metadataGroup={metadataGroup} />
  )}
</Story>

## ArgsTable

<ArgsTable of={FormDocumentMetadataGroupComponent} />

import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import { FormGroup, FormControl } from '@angular/forms';

import { FormDisplayGroupComponent } from './form-display-group.component';
import { WebsiteUiUiAdminModule } from '../website-ui-ui-admin.module';

<Meta
  title="UI ADMIN/Form Display Group"
  component={FormDisplayGroupComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiUiAdminModule],
    }),
  ]}
/>

export const displayGroup = new FormGroup({
  tileImageIsCentered: new FormControl(null),
});
export const tileImageIsCenteredFormControlName = 'tileImageIsCentered';

# Form Display Group

<Story
  height="500px"
  name="Default"
  args={{
    tileImageIsCenteredFormControlName,
  }}
>
  {(args) => <drp-form-display-group {...args} displayGroup={displayGroup} />}
</Story>

## Args Table

<ArgsTable of={FormDisplayGroupComponent} />

import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import { FormGroup, FormControl } from '@angular/forms';

import { FormKeywordsGroupComponent } from './form-keywords-group.component';

<Meta
  title="UI FORM GROUPS/Form Keywords Group"
  component={FormKeywordsGroupComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule],
    }),
  ]}
  argTypes={{
    keywordAdded: { action: 'keywordAdded' },
    keywordsDeleted: { action: 'keywordsDeleted' },
  }}
/>

export const formKeywordsGroup = new FormGroup({
  keywords: new FormControl(null),
});

# Form Keywords Group

<Story
  height="500px"
  name="Default"
  args={{
    keywordsFormControlName: 'keywords',
    keywords: ['Photography'],
  }}
>
  {(args) => (
    <drp-form-keywords-group {...args} formKeywordsGroup={formKeywordsGroup} />
  )}
</Story>

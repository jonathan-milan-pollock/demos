import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import { FormGroup, FormControl } from '@angular/forms';

import { NofiyMeFormComponent } from './notify-me-form.component';
import { WebsiteUiModule } from '../website-ui.module';

<Meta
  title="UI Home/Notify Me Form"
  component={NofiyMeFormComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiModule],
    }),
  ]}
/>

export const imagePostFormGroup = new FormGroup({
  title: new FormControl(null),
  text: new FormControl(null),
});

# Image Post Form

<Story height="500px" name="Default">
  {(args) => <drp-notify-me-form imagePostFormGroup={imagePostFormGroup} />}
</Story>

## Args Table

<ArgsTable of={ImagePostFormComponent} />

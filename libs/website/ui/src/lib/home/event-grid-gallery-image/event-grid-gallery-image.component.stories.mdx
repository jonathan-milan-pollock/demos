import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

import { EventGridGalleryImageComponent } from './event-grid-gallery-image.component';
import { WebsiteUiModule } from '../../website-ui.module';

<Meta
  title="UI HOME/Event Grid Gallery Image"
  component={EventGridGalleryImageComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiModule],
    }),
  ]}
/>

# Event Grid Gallery Image

<Story height="500px" name="Default">
  {(args) => <drp-event-grid-gallery-image {...args} />}
</Story>

## Args Table

<ArgsTable of={EventGridGalleryImageComponent} />

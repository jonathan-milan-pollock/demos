import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

import { TopNavBarComponent } from './top-nav-bar.component';
import { WebsiteUiModule } from '../../website-ui.module';

<Meta
  title="UI SHELL/Top Nav Bar"
  component={TopNavBarComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiUiShellModule],
    }),
  ]}
/>

# Top Nav Bar

<Story name="Default">{(args) => <drp-top-nav-bar {...args} />}</Story>

## Args Table

<ArgsTable of={TopNavBarComponent} />

import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

import { TitleBarComponent } from './title-bar.component';
import { WebsiteUiModule } from '../../website-ui.module';

<Meta
  title="UI SHELL/Title Bar"
  component={TitleBarComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiUiShellModule],
    }),
  ]}
/>

# Title Bar

<Story height="500px" name="Default">
  {(args) => <drp-title-bar {...args} />}
</Story>

## Args Table

<ArgsTable of={TitleBarComponent} />

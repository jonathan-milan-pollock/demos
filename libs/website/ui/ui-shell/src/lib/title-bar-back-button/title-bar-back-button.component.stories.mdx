import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

import { TitleBarBackButtonComponent } from './title-bar-back-button.component';
import { WebsiteUiUiShellModule } from '../website-ui-ui-shell.module';

<Meta
  title="UI SHELL/Title Bar Back Button"
  component={TitleBarBackButtonComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiUiShellModule],
    }),
  ]}
/>

# Title Bar Back Button

<Story height="500px" name="Default">
  {(args) => <drp-title-bar-back-button {...args} />}
</Story>

## Args Table

<ArgsTable of={TitleBarBackButtonComponent} />

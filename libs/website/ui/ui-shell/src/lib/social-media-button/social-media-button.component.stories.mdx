import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import {
  faFacebookF,
  faFacebookSquare,
  faInstagram,
  faLinkedin,
  faYoutube,
} from '@fortawesome/free-brands-svg-icons';

import { SocialMediaButtonComponent } from './social-media-button.component';
import { WebsiteUiUiShellModule } from '../website-ui-ui-shell.module';

<Meta
  title="UI SHELL/Social Media Button"
  component={SocialMediaButtonComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiUiShellModule],
    }),
  ]}
  argTypes={{
    faIcon: {
      options: ['Facebook', 'Instagram', 'LinkedIn', 'YouTube'],
      mapping: {
        Facebook: faFacebookSquare,
        Instagram: faInstagram,
        LinkedIn: faLinkedin,
        YouTube: faYoutube,
      },
    },
    clicked: { action: 'clicked' },
  }}
/>

# Social Media Button

## Facebook

<Story
  name="Facebook"
  args={{
    ariaLabel: 'Share Image with Dark Rush Photography on Facebook',
  }}
>
  {(args) => <drp-social-media-button {...args} faIcon={faFacebookF} />}
</Story>

## Facebook Square

<Story
  name="Facebook Square"
  args={{
    ariaLabel: 'Find out more about Dark Rush Photography on Facebook',
  }}
>
  {(args) => <drp-social-media-button {...args} faIcon={faFacebookSquare} />}
</Story>

## Instagram

<Story
  name="Instagram"
  args={{
    ariaLabel: 'Find out more about Dark Rush Photography on Instagram',
  }}
>
  {(args) => <drp-social-media-button {...args} faIcon={faInstagram} />}
</Story>

## LinkedIn

<Story
  name="LinkedIn"
  args={{
    ariaLabel: 'Find out more about Dark Rush Photography on LinkedIn',
  }}
>
  {(args) => <drp-social-media-button {...args} faIcon={faLinkedin} />}
</Story>

## YouTube

<Story
  name="YouTube"
  args={{
    ariaLabel: 'Watch videos from Dark Rush Photography on YouTube',
  }}
>
  {(args) => <drp-social-media-button {...args} faIcon={faYoutube} />}
</Story>

## Args Table

<ArgsTable of={SocialMediaButtonComponent} />

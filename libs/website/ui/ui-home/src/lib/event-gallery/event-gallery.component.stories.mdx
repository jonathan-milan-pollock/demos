import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

import { EventGalleryComponent } from './event-gallery.component';
import { WebsiteUiUiHomeModule } from '../website-ui-ui-home.module';

<Meta
  title="UI HOME/Event Gallery"
  component={EventGalleryComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiUiHomeModule],
    }),
  ]}
/>

# Event Gallery

<Story height="500px" name="Default">
  {(args) => <drp-event-gallery {...args} />}
</Story>

## Args Table

<ArgsTable of={EventGalleryComponent} />

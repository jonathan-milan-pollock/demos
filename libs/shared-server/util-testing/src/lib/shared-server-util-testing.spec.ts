import { sharedServerUtilTesting } from './shared-server-util-testing';

describe('sharedServerUtilTesting', () => {
  it('should work', () => {
    expect(sharedServerUtilTesting()).toEqual('shared-server-util-testing');
  });
});

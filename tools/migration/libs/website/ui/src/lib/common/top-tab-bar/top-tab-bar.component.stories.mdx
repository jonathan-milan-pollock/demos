import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

import { TopTabBarComponent } from './top-tab-bar.component';
import { WebsiteUiModule } from '../../website-ui.module';

<Meta
  title="UI SHELL/Top Tab Bar"
  component={TopTabBarComponent}
  decorators={[
    moduleMetadata({
      imports: [BrowserModule, BrowserAnimationsModule, WebsiteUiUiShellModule],
    }),
  ]}
/>

# Top Tab Bar

<Story name="Default">{(args) => <drp-top-tab-bar {...args} />}</Story>

## Args Table

<ArgsTable of={TopTabBarComponent} />

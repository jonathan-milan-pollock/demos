import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { BrowserModule } from '@angular/platform-browser';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

import { ImageGridGalleryComponent } from './image-grid-gallery.component';
import { ElementsUiImageGridGalleryModule } from '../elements-ui-image-grid-gallery.module';

<Meta
  title="UI CUSTOM ELEMENTS/Image Grid Gallery"
  component={ImageGridGalleryComponent}
  decorators={[
    moduleMetadata({
      imports: [
        BrowserModule,
        BrowserAnimationsModule,
        ElementsUiImageGridGalleryModule,
      ],
    }),
  ]}
/>

# Image Grid Gallery

<Story name="Default">{(args) => <drp-image-grid-gallery {...args} />}</Story>

## Args Table

<ArgsTable of={ImageGridGalleryComponent} />

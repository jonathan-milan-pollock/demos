import { IconProp } from '@fortawesome/fontawesome-svg-core';

export default class Test {
  static readonly FONT_AWESOME_ICON: IconProp = {
    prefix: 'fab',
    iconName: 'facebook-square',
  };
}
